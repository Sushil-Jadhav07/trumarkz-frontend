import React from 'react'; import { motion } from 'framer-motion'; import { AuthLayout } from '@/components/layout/AuthLayout'; import { PageHeader } from '@/components/shared/PageHeader'; import { Card } from '@/components/ui/Card'; import { Badge } from '@/components/ui/Badge';
const apis = [{ name: 'Aadhaar API', status: 'operational', uptime: 99.9, latency: '120ms' }, { name: 'PAN API', status: 'operational', uptime: 99.7, latency: '95ms' }, { name: 'DL API', status: 'operational', uptime: 98.5, latency: '140ms' }, { name: 'NAD (Education)', status: 'degraded', uptime: 91.2, latency: '850ms' }, { name: 'EPFO API', status: 'operational', uptime: 99.1, latency: '200ms' }, { name: 'GST API', status: 'operational', uptime: 99.8, latency: '110ms' }, { name: 'Dhiway Blockchain', status: 'operational', uptime: 100, latency: '340ms' }, { name: 'Razorpay', status: 'operational', uptime: 99.9, latency: '180ms' }];
export const PlatformHealth = () => <AuthLayout title="Platform Health"><PageHeader title="Platform Health" subtitle="Live status of all API integrations and services" /><motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}><Card className="p-5"><div className="space-y-3">{apis.map((api, i) => <motion.div key={api.name} initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: i * 0.06 }} className="flex items-center gap-4 p-3 rounded-xl bg-gray-50"><div className={`w-2 h-2 rounded-full shrink-0 ${api.status === 'operational' ? 'bg-green-500' : 'bg-orange-400'}`} /><div className="flex-1 min-w-0"><div className="flex items-center justify-between mb-1"><p className="text-sm font-medium text-brand-dark font-inter">{api.name}</p><Badge status={api.status === 'operational' ? 'verified' : 'warning'}>{api.status === 'operational' ? 'Operational' : 'Degraded'}</Badge></div><div className="flex gap-4 text-xs text-gray-400 font-inter"><span>Uptime: {api.uptime}%</span><span>Latency: {api.latency}</span></div></div></motion.div>)}</div></Card></motion.div></AuthLayout>; export default PlatformHealth;
