import React, { useState } from 'react'; import { motion } from 'framer-motion'; import { AuthLayout } from '@/components/layout/AuthLayout'; import { PageHeader } from '@/components/shared/PageHeader'; import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { Input } from '@/components/ui/Input'; import { Link2, MessageCircle, Download, Copy, CheckCircle } from 'lucide-react'; import toast from 'react-hot-toast';
export const ShareProfile = () => { const [copied, setCopied] = useState(false); const profileUrl = 'https://trumarkz.com/verify/individual/IND-2026-00823'; const handleCopy = () => { navigator.clipboard.writeText(profileUrl); setCopied(true); toast.success('Link copied!'); setTimeout(() => setCopied(false), 3000); }; const shareOptions = [{ icon: Link2, label: 'Copy Profile Link', sublabel: 'Share anywhere — email, LinkedIn, job portals', action: handleCopy }, { icon: MessageCircle, label: 'Share via WhatsApp', sublabel: 'Opens WhatsApp with your profile link', action: () => window.open(`https://wa.me/?text=${encodeURIComponent('My TruMarkZ verified profile: ' + profileUrl)}`, '_blank') }, { icon: Download, label: 'Download as PDF', sublabel: 'PDF with tappable QR on each credential node', action: () => toast.success('Generating PDF...') }]; return <AuthLayout title="Share Profile"><PageHeader title="Share My Skill Tree" subtitle="Let recruiters, hospitals, and employers verify you instantly" /><motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}><Card className="p-5 mb-4"><p className="text-xs text-gray-500 font-inter mb-2">Your profile link</p><div className="flex items-center gap-2"><Input value={profileUrl} onChange={() => {}} className="flex-1" /><Button variant={copied ? 'success' : 'outline'} size="sm" icon={copied ? CheckCircle : Copy} onClick={handleCopy}>{copied ? 'Copied' : 'Copy'}</Button></div></Card><div className="space-y-3">{shareOptions.map((opt, i) => <motion.div key={opt.label} initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: i * 0.08 }}><Card className="p-4 hover:shadow-md transition-shadow cursor-pointer" onClick={opt.action}><div className="flex items-center gap-4"><div className="p-3 bg-brand-blue/10 rounded-xl"><opt.icon size={20} className="text-brand-blue" /></div><div className="flex-1"><p className="font-inter font-semibold text-sm text-brand-dark">{opt.label}</p><p className="text-xs text-gray-400 font-inter">{opt.sublabel}</p></div></div></Card></motion.div>)}</div></motion.div></AuthLayout>; }; export default ShareProfile;
